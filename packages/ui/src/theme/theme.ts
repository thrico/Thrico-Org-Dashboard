import React from "react";

const theme = {
  components: {
    Button: {
      borderRadius: 7,
      // Enable algorithm
    },
    Input: {
      borderRadius: 7,
      // Enable algorithm
    },
  },
  token: {
    borderRadiusLG: 10,
    // Seed Token
    colorPrimary: "#013DC4",
    borderRadius: 2,
    fontSize: 14,
    fontFamily: "'Work Sans', sans-serif;",
    // Alias Token
    colorBgContainer: "#ffffff",
  },
};

export default theme;
